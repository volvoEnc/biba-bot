
| 💬 Локальный рейтинг по фапам на данный момент! <br><br>

- for ( let i = 0; i < users.length; i++ )
  - var count_fap = plural(users[i].count_fap, '%d раз', '%d раза', '%d раз');
  - var place = offset + i + 1;
  case i
    when 0
      | #{place}. [id#{users[i].id}|#{users[i].first_name} #{users[i].last_name}]. Биба :  #{count_fap} <br>
    when 1
      | #{place}. [id#{users[i].id}|#{users[i].first_name} #{users[i].last_name}]. Биба :  #{count_fap} <br>
    when 2
      | #{place}. [id#{users[i].id}|#{users[i].first_name} #{users[i].last_name}]. Биба :  #{count_fap} <br>
    when 3
      | #{place}. [id#{users[i].id}|#{users[i].first_name} #{users[i].last_name}]. Биба :  #{count_fap} <br>
    when 4
      | #{place}. [id#{users[i].id}|#{users[i].first_name} #{users[i].last_name}]. Биба :  #{count_fap} <br>
